{
  "type": "module",
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "clean-css": "^5.3.3",
    "glob": "^13.0.0",
    "uglify-js": "^3.19.3"
  },
  "scripts": {
    "watch": "chokidar 'assets/js/**/*.js' 'assets/css/**/*.css' -i 'assets/**/*.min.*' -c 'npm run prod'",
    "prod": "node scripts/minify-all.js"
  }
}
